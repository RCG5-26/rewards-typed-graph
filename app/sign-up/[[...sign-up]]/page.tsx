import GoogleAuthCard from "@/components/auth/GoogleAuthCard";

export default function SignUpPage() {
  return (
    <GoogleAuthCard
      heading="Create your GPFree account"
      subheading="Continue with Google — we'll set up your account on first login."
    />
  );
}
